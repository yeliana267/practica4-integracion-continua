name: Alerta CI (ntfy)

on:
  push:
    branches: ["main"]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar notificación a ntfy.sh
        run: |
          curl -sS -X POST \
            -H "Title: Push a main" \
            -H "Priority: 4" \
            -H "Tags: github,ci,devops" \
            -d "Repositorio: ${{ github.repository }} | Autor: Yeliana Diaz 2024-0196" \
            https://ntfy.sh/devops-itla
